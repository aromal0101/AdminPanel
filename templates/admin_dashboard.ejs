<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        /* Base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f8f9fa;
            color: #333;
        }
        
        /* Header styles */
        .header {
            background: linear-gradient(to right, #3a7bd5, #3a6073);
            color: white;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .header h1 {
            font-size: 1.8rem;
            font-weight: 600;
        }
        
        .header-buttons {
            display: flex;
            gap: 10px;
        }
        
        .btn {
            padding: 0.6rem 1.2rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }
        
        .logout-btn {
            color: white;
            background: #f44336;
        }
        
        .logout-btn:hover {
            background: #d32f2f;
            transform: translateY(-2px);
            box-shadow: 0 3px 5px rgba(0,0,0,0.2);
        }
        
        /* Main container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }
        
        .section-title {
            color: #2c3e50;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid #3a7bd5;
        }
        
        .action-buttons {
            display: flex;
            gap: 10px;
        }
        
        .export-btn {
            color: white;
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
        }
        
        .excel-btn {
            background: #217346; /* Excel green color */
        }
        
        .excel-btn:hover {
            background: #1e6a41;
        }
        
        .csv-btn {
            background: #FF9800;
        }
        
        .csv-btn:hover {
            background: #F57C00;
        }
        
        .json-btn {
            background: #673AB7;
        }
        
        .json-btn:hover {
            background: #5E35B1;
        }
        
        /* Table styles */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
        }
        
        .data-table th {
            background: #3a7bd5;
            color: white;
            padding: 12px 15px;
            text-align: left;
            font-weight: 600;
        }
        
        .data-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
            color: #333;
        }
        
        .data-table tr:last-child td {
            border-bottom: none;
        }
        
        .data-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        .data-table tr:hover {
            background-color: #f1f7ff;
        }
        
        /* Token styles */
        .token-text {
            max-width: 300px;
            overflow: hidden;
            text-overflow: ellipsis;
            font-family: monospace;
            background: #f1f1f1;
            padding: 2px 5px;
            border-radius: 3px;
        }
        
        /* Responsive design */
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                text-align: center;
                padding: 1rem;
                gap: 0.8rem;
            }
            
            .container {
                padding: 1rem;
            }
            
            .section-header {
                flex-direction: column;
                gap: 1rem;
                align-items: flex-start;
            }
            
            .action-buttons {
                flex-wrap: wrap;
            }
            
            .data-table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }
            
            .data-table th, 
            .data-table td {
                padding: 8px 10px;
            }
            
            .token-text {
                max-width: 150px;
            }
        }
        
        /* Table container to prevent scrolling */
        .table-container {
            overflow-x: auto;
            max-height: calc(100vh - 200px);
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Admin Dashboard</h1>
        <div class="header-buttons">
            <a href="/admin/logout" class="btn logout-btn">
                <span>Logout</span>
            </a>
        </div>
    </div>
    
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Google Tokens</h2>
            <div class="action-buttons">
                <a href="/admin/download/excel" class="btn export-btn excel-btn">
                    <span>Export Excel</span>
                </a>
                <a href="/admin/download/csv" class="btn export-btn csv-btn">
                    <span>Export CSV</span>
                </a>
                <a href="/admin/download/json" class="btn export-btn json-btn">
                    <span>Export JSON</span>
                </a>
            </div>
        </div>
        
        <div class="table-container">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Email</th>
                        <th>Access Token</th>
                    </tr>
                </thead>
                <tbody>
                    <% tokens.forEach(function(token) { %>
                    <tr>
                        <td><%= token.email %></td>
                        <td><div class="token-text"><%= token.access_token %></div></td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>